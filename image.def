Bootstrap: docker
From: tensorflow/tensorflow:latest-gpu-py3

%files
    ./requirements.txt
    ./launcher

%post
  pip install -r requirements.txt
  ls -al
  ls -al launcher
  pip install file:./launcher
  rm -rf launcher
  mkdir /data
